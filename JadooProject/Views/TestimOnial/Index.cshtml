@using JadooProject.Features.Mediator.Results.TestimOnialResults
@model List<GetTestimOnialQueryResult>

@{
    ViewData["Title"] = "Referanslar";
}

<div class="container mt-4">
    <h3>Referanslar</h3>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>ID</th>
                <th>Ad Soyad</th>
                <th>Lokasyon</th>
                <th>Yorum</th>
                <th>Görsel</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.TestimonialId</td>
                    <td>@item.Name</td>
                    <td>@item.Location</td>
                    <td>@item.Comment</td>
                    <td><img src="@item.ImageUrl" width="100" height="100" style="object-fit: cover;" /></td>
                    <td>
                        <a href="/TestimOnial/Update/@item.TestimonialId" class="btn btn-warning btn-sm">Güncelle</a>
                        <a href="/TestimOnial/Delete/@item.TestimonialId" onclick="return confirm('Silmek istediğinize emin misiniz?')" class="btn btn-danger btn-sm">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <a href="/Testimonial/Create" class="btn btn-primary">Yeni Testimonial Ekle</a>
</div>
